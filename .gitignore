.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
Cara Backup.txt

# Database backup (large files)
*.sql
esimko_backup.sql
esimko_prod_backup.sql

# Storage (logs and cache - large files)
storage/logs/
storage/framework/cache/

# Docker
mysql_data/

# IDE
.idea/
.vscode/

# HTML folder (copied from server, might have large files)
/html/

# EsimkoMobile Android build artifacts
EsimkoMobile/.gradle/
EsimkoMobile/**/build/
EsimkoMobile/local.properties
EsimkoMobile/app/build/
# APK build artifacts
app-debug*.apk
# env secrets (guardrail: jangan commit)
.env.local
.env.production.key
.env.backup.key
storage/framework/views/
storage/framework/cache/
storage/framework/sessions/

# public/storage = artisan storage:link symlink -> storage/app/public.
# ponytail: git tracks real files here = conflicts with symlink, new uploads 404.
public/storage/
